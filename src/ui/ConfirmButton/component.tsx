import React from "react";
import styled from "styled-components";

const ConfirmButtonComponent: React.FC = () => {
  return <div className="App" />;
};

export const ConfirmButton = styled(ConfirmButtonComponent)`
  background-color: grey;
`;
